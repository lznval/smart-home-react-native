import {Text, View} from "react-native";
import Layout from "../components/Layout";

const Home = ({navigation}) => {
    return (
        <Layout>
            <Text>This is a test app for "Smart home"</Text>
        </Layout>
    );
};

export default Home;